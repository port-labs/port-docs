name: Check for YAML File Changes

on:
  schedule:
    - cron: '0 0 * * *'  # Runs daily at midnight UTC
  workflow_dispatch:       # Allows manual triggering

permissions:
  contents: write          # Grants write permissions for the GITHUB_TOKEN

jobs:
  check-diff:
    runs-on: ubuntu-latest

    steps:
    # Step 1: Get the current date
    - name: Get current date
      id: date
      run: echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

    # Step 2: Checkout the repository
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 0  # Fetch all history for accurate diff

    # Step 3: Download the YAML file from the URL
    - name: Download YAML file
      run: curl -sL https://api.getport.io/yaml -o newSpec.yaml

    # Step 4: Check for differences between the new file and the existing file
    - name: Check for differences
      id: diff_check
      run: |
        if cmp -s newSpec.yaml static/rawApiSpec.yaml; then
          echo "diff_found=false" >> $GITHUB_OUTPUT
          echo "No changes detected."
        else
          echo "diff_found=true" >> $GITHUB_OUTPUT
          echo "Differences detected!"
        fi

    # Step 5: Update the YAML file in the repository if differences are found
    - name: Update YAML file
      if: steps.diff_check.outputs.diff_found == 'true'
      run: cp newSpec.yaml static/rawApiSpec.yaml

    # Step 6: Create a pull request with the changes
    - name: Create Pull Request
      if: steps.diff_check.outputs.diff_found == 'true'
      uses: peter-evans/create-pull-request@v5
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        branch: update-${{ steps.date.outputs.date }}
        commit-message: "Update YAML file (update-${{ steps.date.outputs.date }})"
        title: "Update YAML file (update-${{ steps.date.outputs.date }})"
        body: "This PR updates the YAML file to the latest version."
