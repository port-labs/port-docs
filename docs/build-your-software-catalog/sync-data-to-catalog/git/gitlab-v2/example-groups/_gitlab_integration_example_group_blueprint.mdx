```json
{
  "identifier": "gitlabGroup",
  "description": "This blueprint represents a GitLab group",
  "title": "GitLab Group",
  "icon": "GitLab",
  "schema": {
    "properties": {
      "url": {
        "type": "string",
        "title": "URL",
        "description": "Link to the group in GitLab"
      },
      "visibility": {
        "type": "string",
        "title": "Visibility",
        "description": "The visibility of the group",
        "enum": ["private", "internal", "public"]
      },
      "description": {
        "type": "string",
        "title": "Description",
        "description": "The description of the group"
      }
    },
    "required": []
  }
}
```