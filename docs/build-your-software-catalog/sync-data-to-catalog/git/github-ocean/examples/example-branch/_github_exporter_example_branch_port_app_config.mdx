<details>
<summary>Port port-app-config.yml</summary>

```yaml showLineNumbers
repositoryType: 'all'
resources:
  - kind: repository
    selector:
      query: 'true'
    port:
      entity:
        mappings:
          identifier: .name
          title: .name
          blueprint: '"githubRepository"'
          properties:
            readme: file://README.md
            url: .html_url
            defaultBranch: .default_branch
  - kind: branch
    selector:
      query: 'true'
    port:
      entity:
        mappings:
          identifier: .__repository + "_" + .branch.name
          title: .__repository + " " + .branch.name
          blueprint: '"githubBranch"'
          properties:
            is_protected: .branch.protected
            commit_sha: .branch.commit.sha
            commit_url: .branch.commit.url
          relations:
            repository: .__repository
```
</details>
