<details>

<summary><b> Port port-app-config.yml (click to expand) </b></summary>

```yaml showLineNumbers
createMissingRelatedEntities: true
resources:
  - kind: repository
    selector:
      query: "true" # JQ boolean query. If evaluated to false - skip syncing the object.
    port:
      entity:
        mappings:
          identifier: .name
          title: .name
          blueprint: '"githubRepository"'
          properties:
            readme: file://README.md
            url: .html_url
            defaultBranch: .default_branch

  - kind: repository
    selector:
      query: "true"
      include: ["sbom"] # Include sbom as a relationship
    port:
      itemsToParse: .__sbom.packages
      itemsToParseTopLevelTransform: true
      entity:
        mappings:
          identifier: .item.SPDXID
          title: .item.name
          blueprint: '"githubSBOM"'
          properties:
            name: .item.name
            spdxid: .item.SPDXID
            version: .item.versionInfo
            license_concluded: .item.licenseConcluded
            copyright_text: .item.copyrightText
            external_refs: .item.externalRefs
          relations:
            repository: .name
```

</details>
