:::note Prerequisites

- An existing Relation between two blueprints.

:::

It is possible to map relations in the entity definition written in the `port.yml` file, simply use the `relations` key in the `yml` file as shown in the [examples](#examples) below.
